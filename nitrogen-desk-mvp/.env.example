# Caminho do banco SQLite. No Railway, se voce adicionar um Volume, aponte para ele
# (ex: /data/data.sqlite) para o banco sobreviver a redeploys. Sem Volume, os dados
# somem a cada novo deploy.
DB_PATH=./data.sqlite

# Porta (Railway define isso automaticamente, nao precisa mexer)
PORT=3000
